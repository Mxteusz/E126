<%- include('../partials/header') %>

    <h2>Edytuj kartę</h2>
    <form action="/edit/<%= card._id %>" method="POST">
        <input type="text" name="name" value="<%= card.name %>" required><br>
        <input type="text" name="rarity" value="<%= card.rarity %>" required><br>
        <input type="number" name="elixir" value="<%= card.elixir %>" required><br>
        <textarea name="opinion"><%= card.opinion %></textarea><br>
        <input type="password" name="password" value="<%= card.password %>" required><br>
        <button type="submit">Zapisz zmiany</button>
    </form>

<%- include('../partials/footer') %>